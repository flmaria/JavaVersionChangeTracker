package io.github.flmaria.java_016.vectorapi;

//import jdk.incubator.vector.FloatVector;
//import jdk.incubator.vector.VectorSpecies;

public class Main {

    //@TODO: is not compiling - package jdk.incubator.vector is not visible

    /*
    The Vector API provides a mechanism to express computations that can be optimized at runtime by the JVM, leveraging the hardware's vector instructions.
    Vector instructions are capable of performing operations on multiple data elements simultaneously, which can significantly improve the performance of data-parallel computations.
     */

//    private static final VectorSpecies<Float> SPECIES = FloatVector.SPECIES_256;

    public static void main(String[] args) {
//        float[] a = {1.0f, 2.0f, 3.0f, 4.0f};
//        float[] b = {5.0f, 6.0f, 7.0f, 8.0f};
//        float[] result = new float[4];
//
//        // Load data into vector
//        FloatVector va = FloatVector.fromArray(SPECIES, a, 0);
//        FloatVector vb = FloatVector.fromArray(SPECIES, b, 0);
//
//        // Perform vectorized addition
//        FloatVector vr = va.add(vb);
//
//        // Store the result back to array
//        vr.intoArray(result, 0);
//
//        // Print the result
//        for (float r : result) {
//            System.out.println(r);
//        }
    }

}
